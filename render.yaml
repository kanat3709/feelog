services:
  - type: web
    name: feelog
    runtime: ruby
    buildCommand: bundle install; bundle exec rails db:migrate; bundle exec rake assets:precompile; bundle exec rake assets:clean;
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false