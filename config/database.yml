default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: feelog_development

test:
  <<: *default
  database: feelog_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>